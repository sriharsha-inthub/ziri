name: Publish Ziri to NPM

on:
  push:
    branches: [main]
    # optional: tags: ['v*']

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          registry-url: 'https://registry.npmjs.org'

      - name: Install system dependencies for faiss-node
        run: |
          sudo apt-get update
          sudo apt-get install -y libblas-dev liblapack-dev cmake build-essential

      - name: Configure npm authentication
        run: echo "//registry.npmjs.org/:_authToken=${{ secrets.NPM_TOKEN }}" > ~/.npmrc

      - name: Fix npm optional dependencies bug
        run: |
          cd packages/ziri-js
          rm -rf node_modules package-lock.json
          npm install

      - name: Bump version (patch)
        run: |
          cd packages/ziri-js
          npm version patch -m "chore: bump version to %s [skip ci]"

      - name: Publish to NPM
        run: |
          cd packages/ziri-js
          npm publish --access public

      - name: Push version bump
        uses: ad-m/github-push-action@v0.6.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: main
          force: false
