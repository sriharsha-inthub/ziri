# Ziri (PyPI shim) v{{VERSION_RANGE}}

This is a small Python wrapper that invokes the `ziri` Node CLI.
Requires Node.js **>= 18** on PATH.

Install:
```bash
pipx install ziri
# or
pip install ziri
```
